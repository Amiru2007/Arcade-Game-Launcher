<Page
    x:Class="WinUIClient.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUIClient.Views">

    <Grid>
        <Border
            Width="400"
            Padding="40"
            Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
            CornerRadius="12"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">

            <StackPanel>
                <TextBlock
                    Text="Sign in"
                    FontSize="28"
                    FontWeight="Bold"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,24"/>

                <TextBox
                    x:Name="EmailTextBox"
                    Header="Email"
                    InputScope="EmailNameOrAddress"
                    PlaceholderText="you@example.com"
                    Margin="0,0,0,16"/>

                <PasswordBox
                    x:Name="PasswordBox"
                    Header="Password"
                    Margin="0,0,0,24"/>

                <Button
                    Content="Sign In"
                    Click="OnLoginClick"
                    HorizontalAlignment="Stretch"
                    Style="{StaticResource AccentButtonStyle}" />

            </StackPanel>
        </Border>
    </Grid>
</Page>
